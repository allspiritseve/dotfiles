#!/bin/bash

ssh="ssh -t deploy@thorin"

case "$1" in
  r|run)
    $ssh $@ --app townstage.com
    ;;
  rake)
    $ssh run $@ --app townstage.com
    ;;
  c|console)
    $ssh run rails console --app townstage.com
    ;;
  s|shell)
    $ssh
esac
